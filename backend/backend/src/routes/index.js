import { Router } from 'express';
// Importe suas rotas de recursos aqui, exemplo:
// import userRoutes from './user.routes.js';

const router = Router();

// router.use('/users', userRoutes);

export default router; 